#include<cstdio>
using namespace std;

int main() {
	int tc;
	scanf("%d", &tc);
	for (int i = 0; i < tc; i++) {
		int n, m;
		scanf("%d%d", &n, &m);
		for (int j = 0; j < m; j++) {
			int a, b;
			scanf("%d%d", &a, &b);
		}
		printf("%d\n", n - 1);
	}

	return 0;
}